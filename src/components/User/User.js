const User = ({user}) => {
const {name, position, photo, email} = user

    return (
        <div>
                <div>
                    <img src="" alt=""/>
                    
                </div>
        </div>
    );
};

export {User};